The `auto_markdown` flag on `Node` enables some basic Markdown support.

This only covers basic styling (**Bold**, _italic_, and `inline code`) and paragraph breaks.
To render Markdown properly we'd need to split the text into multiple separate Nodes. 

This will probably implemented as a `Component` at some point in the future.
